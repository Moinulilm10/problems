#include<bits/stdc++.h>
using namespace std;

#define MAX 30010;

vector<int>edge[MAX], cost[MAX];
int t, cs, n, m, u, v, w, mN, mDis

int main()
{
    int t, n, u, v, w;
    scanf("%d",&t);
    while(t--)
    {
        scanf("%d",&n);
        for(int i=0; i<n-1; i++)
        {
            scanf("%d %d %d",&u, &v, &w);
        }
    }
}
